filebeat.inputs:
- type: log
  enabled: true
  fields:
    application: "Docker"
    fields_under_root: true
  paths:
    - /var/lib/docker/containers/*/*.log

output.logstash:
  hosts: ["logstash:5044"]
